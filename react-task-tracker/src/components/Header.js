// import React from 'react'
import Proptyoes  from 'prop-types'
const Header = ({title}) => {
  return (
    <header>
      <h1>{title}</h1>
    </header>
  ) 
}


Header.defaultProps  = {
  title :"Task Tracker.."

}


Header. defaultPr
export default Header
